# The FastAPI server. The websocket URL is derived from this.
VITE_API_BASE=http://localhost:8000

# Optional. Without it the admin controls are not rendered at all, rather than
# shown as buttons that would 401.
VITE_ADMIN_TOKEN=
