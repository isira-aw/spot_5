# ── core platform ────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
SQLAlchemy>=2.0             # the database is SQLite, from the standard library
requests>=2.31
python-dotenv>=1.0

# ── engine_3 (risk model) ────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# ── optional, per component ──────────────────────────────────────────────────
# engine_2 training and in-process inference (TensorFlow, ccxt, PyWavelets) has
# its own file, because TensorFlow is a ~600MB install the API does not need:
#   pip install -r backend/engine_2/requirements.txt
# Not needed at all when ENGINE_2_SOURCE=file — the model then runs elsewhere and
# this process only tails its decision log.
#
# REAL mode order placement:
#   ccxt>=4.2
# tests:
#   pytest>=8.0
