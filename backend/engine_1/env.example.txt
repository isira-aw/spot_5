# ---- LLM (primary) ----------------------------------------------
GROQ_API_KEY=gsk_your_key_here
# If this model isn't served by your account the script auto-picks a
# working one from the list in GROQ_FALLBACK_MODELS.
GROQ_MODEL=qwen/qwen3-32b
GROQ_BASE_URL=https://api.groq.com/openai/v1

# ---- LLM (local fallback) ---------------------------------------
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5:0.5b

# ---- News (optional; script runs fine without it) ---------------
ALPHA_VANTAGE_API_KEY=

# ---- Misc -------------------------------------------------------
PAIR=XBTUSDT
HTTP_TIMEOUT=25
LLM_TIMEOUT=90
